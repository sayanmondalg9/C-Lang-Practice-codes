#include <stdio.h>

// Do While loop is a While loop which runs at least one time

int main()
{
    int a = 0;
    do
    {
        printf("a = %d\n", a);
        a++;
    } while (a<10);
    
    return 0;
}